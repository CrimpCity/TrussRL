"""Problem instance definition.

``TrussInstance`` carries every randomized quantity verbatim: span, load
cases, depth limit, deflection denominator, and grading parameters such as
``cost_ref`` — everything the prompt shows plus everything the verifier
needs to grade a completion.
"""
