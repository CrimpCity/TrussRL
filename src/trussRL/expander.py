"""Geometry expander (starting.md Module 2).

Expands a validated design dict into nodes, members, supports, and
unbraced lengths, dispatching on typology (Warren first). The single
geometry source of truth shared by the reward pipeline, calibration,
heuristic baseline, and eval harness (vision.md section 5).
"""
