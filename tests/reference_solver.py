"""Test-only reference truss solver (vision.md section 7).

A small (~80 line) numpy direct-stiffness solver, independent of
OpenSees, used by the differential test to cross-check member forces and
deflections. Never imported by ``src/``.
"""
